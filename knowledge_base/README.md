# Knowledge base

Synthetic internal documents for **NovaTel**, a fictional telecom operator.
They are indexed by `python -m src.rag.ingest` and retrieved by the agent's
`search_knowledge_base` tool. All plans, prices and policies are invented for
demonstration. Replace them with real documents to adapt the system.

This README is excluded from the index.
